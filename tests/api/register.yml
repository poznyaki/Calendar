info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/register
  body:
    type: json
    data: |-
      {
        'login': '',
        'email': '',
        'password': ''
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​